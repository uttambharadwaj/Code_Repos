global with sharing class ForteIntegrationButton {

//	webService static void verifyAcctRtNos(Id appId) {
//		List<OnlineApplication__c> apps = [SELECT Application_Stage__c, Forte_Response_Code__c FROM OnlineApplication__c WHERE Id = :appId];
//
//		if (!apps.isEmpty()) {
//			UOnlineApplication.verifyAcctRtNos(apps, true);
//		}
//	}
}