@isTest (SeeAllData=true)
public class CampaignMemberBatchDeleteTest{ 

   static testMethod void runTest(){
  /*
        CampaignMember cm = new CampaignMember();
        cm.campaign = ''     
        cm.contact = 'Peter Sawicki';
        cm.lead = new Lead();
        cm.status = 'Responded';
        insert cm;
        
        String sql = 'SELECT ID FROM CampaignMember WHERE ';            
        List <CampaignMember> campaignMembers = Database.query(sql);
        System.debug('campaignMembers size:' + campaignMembers.size());      
         
        Test.StartTest();
        System.Assert(campaignMembers.size()> 0);
        BatchDelete batchApex = new BatchDelete (sql);
        Database.executeBatch(batchApex);   
           
        Test.StopTest();

        campaignMembers = Database.query(sql);
        System.debug('campaignMembers size NOW:' + campaignMembers.size());       
        System.Assert(campaignMembers.size() == 0); 
       */
    }
}