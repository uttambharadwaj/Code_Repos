global with sharing class UOnlineApplicationExperianCalloutButton {
	public UOnlineApplicationExperianCalloutButton() {
		
	}


	webService static void callExperianBizId(Id onlineAppdlicationId){
	// coment out code below so code coverage is maintained as this call out is no longer need. 
	// per GearsCRM Case 30757 enm 04/18/2017

	/*
		OnlineApplication__c oa = new OnlineApplication__c();
		List<OnlineApplication__c> records = new List<OnlineApplication__c>();
		Map<Id, OnlineApplication__c> oldMap = new Map<Id,OnlineApplication__c>();

		List<Id> oaIds = new List<Id>();
		oaIds.add(onlineAppdlicationId);
		UOnlineApplication.callExperianBizScoreFuture(oaIds);
	*/					   
	}
}