<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <fields>
        <fullName>Pardot_URL__c</fullName>
        <description>Used to identify if campaign member has been synced with Pardot.</description>
        <externalId>false</externalId>
        <formula>IF(
NOT(ISBLANK(Contact.pi__url__c)), HYPERLINK(Contact.pi__url__c, Contact.pi__url__c), 
IF(				
NOT(ISBLANK(Lead.pi__url__c)),HYPERLINK(Lead.pi__url__c,Lead.pi__url__c), 
NULL))</formula>
        <label>Pardot URL</label>
        <required>false</required>
        <type>Text</type>
        <unique>false</unique>
    </fields>
    <recordTypes>
        <fullName>WES_Campaign_Member</fullName>
        <active>true</active>
        <label>WES Campaign Member</label>
    </recordTypes>
</CustomObject>
